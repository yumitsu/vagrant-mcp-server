# MCP Server configuration
MCP_TRANSPORT=stdio        # Transport type: stdio or sse
MCP_PORT=3000              # Port for SSE transport

# Logging configuration
LOG_LEVEL=info             # Logging level: debug, info, warn, error
LOG_FILE=./logs/server.log # Log file path

# Vagrant configuration
VAGRANT_HOME=~/.vagrant.d  # Vagrant home directory
VAGRANT_DEFAULT_PROVIDER=libvirt  # Default provider (libvirt, virtualbox, vmware_desktop, hyperv)

# VM configuration defaults
DEFAULT_VM_BOX=ubuntu/focal64  # Default Vagrant box
DEFAULT_VM_CPU=2               # Default CPU cores
DEFAULT_VM_MEMORY=2048         # Default memory in MB
DEFAULT_VM_SYNC_TYPE=rsync     # Default sync type: rsync, nfs, smb, virtualbox

# VM storage location
VM_BASE_DIR=~/.vagrant-mcp-server/vms  # Base directory for VM files

# Sync configuration
SYNC_POLL_INTERVAL=2000  # File watcher poll interval in ms
